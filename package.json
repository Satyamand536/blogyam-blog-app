{
  "name": "blogyam-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ../backend && npm install",
    "start": "cd backend && npm start",
    "install-all": "npm install && cd backend && npm install && cd ../client && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
